<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Habit – HabitGarden</title>

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 50%, #bbf7d0 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .edit-card {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
      padding: 3rem;
      width: 100%;
      max-width: 500px;
      animation: fadeIn 0.8s ease;
      position: relative;
      overflow: hidden;
    }

    .edit-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, #22c55e 0%, #16a34a 100%);
    }

    h3 {
      background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      font-weight: 600;
      font-size: 1.75rem;
      margin-bottom: 2rem;
      text-align: center;
    }

    label {
      color: #1e293b;
      font-weight: 500;
      margin-bottom: 0.5rem;
      display: block;
    }

    .form-control, .form-select {
      border-radius: 12px;
      padding: 0.875rem 1rem;
      border: 2px solid #e2e8f0;
      width: 100%;
      font-size: 1rem;
      transition: all 0.3s ease;
    }

    .form-control:focus, .form-select:focus {
      border-color: #22c55e;
      box-shadow: 0 0 0 3px rgba(34, 197, 94, 0.1);
      outline: none;
    }

    button {
      width: 100%;
      background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 0.875rem;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      margin-top: 0.5rem;
      box-shadow: 0 10px 30px rgba(34, 197, 94, 0.3);
      cursor: pointer;
    }

    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 40px rgba(34, 197, 94, 0.4);
    }

    .text-muted {
      color: #94a3b8;
      font-size: 0.875rem;
      margin-top: 0.25rem;
      display: block;
    }

    .footer-text {
      text-align: center;
      margin-top: 1.5rem;
      color: #64748b;
      font-size: 0.95rem;
    }

    a {
      color: #22c55e;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    a:hover {
      color: #16a34a;
      text-decoration: underline;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="edit-card">
    <h3>Edit Habit 🍃</h3>

    <form action="/edithabit" method="POST">
      <input type="hidden" name="habitid" value="<%= habit.habitid %>">
      <input type="hidden" name="userid" value="<%= userid %>">

      <div class="mb-3">
        <label class="form-label">Habit Name</label>
        <input type="text" class="form-control" name="habitname" value="<%= habit.habitname %>" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Frequency</label>
        <select class="form-select" name="habitfrequency" required>
          <option value="daily" <%= habit.habitfrequency === 'daily' ? 'selected' : '' %>>Daily</option>
          <option value="weekly" <%= habit.habitfrequency === 'weekly' ? 'selected' : '' %>>Weekly</option>
          <option value="monthly" <%= habit.habitfrequency === 'monthly' ? 'selected' : '' %>>Monthly</option>
        </select>
      </div>

      <div class="mb-3">
        <label class="form-label">Category</label>
        <select class="form-select" name="category_id" required>
          <% categories.forEach(function(category) { %>
            <option value="<%= category.category_id %>" <%= habit.category_id == category.category_id ? 'selected' : '' %>>
              <%= category.category_name %>
            </option>
          <% }); %>
        </select>
      </div>

      <div class="mb-3" style="background: #f0fdf4; padding: 1rem; border-radius: 12px; border-left: 4px solid #22c55e;">
        <label class="form-label" style="margin-bottom: 0.5rem;">Current Streak: <strong><%= habit.habitstreak || 0 %> days</strong></label>
        <small class="text-muted" style="display: block;">Growth level automatically updates based on your streak:<br>
          • 0-5 days: 🌱 Seedling<br>
          • 6-15 days: 🌿 Young<br>
          • 16-30 days: 🌳 Mature<br>
          • 31+ days: 🌲 Fully Grown</small>
      </div>

      <button type="submit">Save Changes</button>
    </form>

    <p class="footer-text">
      <a href="/dashboard?userid=<%= userid %>">Back to Dashboard</a>
    </p>
  </div>
</body>
</html>
